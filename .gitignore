.idea

# Large top-level benchmark datasets are stored compressed; leaderboard JSON stays trackable.
results/*.json
!results/*.json.gz

# Local benchmark diagnostics and raw model traces (not leaderboard results).
results/runs/
results/traces/

# Machine-specific benchmark metadata (temp dir paths)
benchmark-runner/datasets/embedding_banks.json

# Local dev config, OS cruft, and branch working notes
.env
.env.*
!.env.example
.venv/
.ruff_cache/
.mypy_cache/
.pytest_cache/
.DS_Store
.claude/
docs/superpowers/
